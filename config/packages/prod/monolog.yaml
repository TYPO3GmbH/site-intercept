monolog:
    handlers:
        main:
            type: group
            members: [file, graylog]
        file:
            type: rotating_file
            level: WARN
            path: "%kernel.logs_dir%/%kernel.environment%.log"
        graylog:
            type: gelf
            level: WARN
            publisher: "gelf.publisher"
