{% extends 'base.html.twig' %}
{% block title %}Discord Webhooks{% endblock %}
{% block headline %}Discord Webhooks{% endblock %}
{% block body %}

    <div class="content-section">
        <div class="container">
            <h2>
                {% if edit is defined %}
                    Edit a Discord message
                {% else %}
                    Add a Discord message
                {% endif %}
            </h2>
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-body">
                            {{ include('discord_scheduled_messages/_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
